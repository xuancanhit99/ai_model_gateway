# Copy this file to ops/.env and fill real values.
# Do NOT commit ops/.env

# --- Remote deploy target ---
REMOTE_HOST=your_server_ip_or_dns
REMOTE_USER=ubuntu
SSH_PORT=22
SSH_KEY_PATH=/absolute/path/to/private_key
REMOTE_STACK_DIR=/opt/stacks/ai_model_gateway
COMPOSE_FILE=compose.yaml

# --- Deploy behavior ---
DEPLOY_MODE=git
GIT_BRANCH=main
SKIP_GIT_PULL=false

# --- Health check on remote backend ---
HEALTH_PATH=/api/v1/health
HEALTH_TIMEOUT_SECONDS=180

# --- Public smoke check ---
FRONTEND_URL=https://gateway.vnpay.dev
API_BASE_URL=https://gateway-api.vnpay.dev
TIMEOUT_SECONDS=20
CHECK_DOCS=false

# --- Optional: source DB for one-time migration script ---
SOURCE_DB_URL=

# --- Playwright login test account (fill to run full authenticated UI flow) ---
TEST_USER_EMAIL=
TEST_USER_PASSWORD=
TEST_USER_OTP=
TEST_USER_EXPECTED_LABEL=
